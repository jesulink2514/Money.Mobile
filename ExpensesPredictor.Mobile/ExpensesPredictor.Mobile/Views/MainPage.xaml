<?xml version="1.0" encoding="utf-8" ?>
<abstract:ViewBase xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ExpensesPredictor.Mobile"
             xmlns:viewModels="clr-namespace:ExpensesPredictor.Mobile.ViewModels;assembly=ExpensesPredictor.Mobile"
             xmlns:abstract="clr-namespace:ExpensesPredictor.Mobile.Views.Abstract;assembly=ExpensesPredictor.Mobile"
             x:Class="ExpensesPredictor.Mobile.MainPage"
             Title="Expenses Predictor">
  <ContentPage.BindingContext>
    <viewModels:MainViewModel/>      
  </ContentPage.BindingContext>
<ContentPage.ToolbarItems>
  <ToolbarItem  Text="New Expense" AutomationId="NewExpense"
                Icon="NewExpensive.png"/>
</ContentPage.ToolbarItems>
  <Grid 
    VerticalOptions="FillAndExpand" 
    HorizontalOptions="FillAndExpand">
    <Grid.RowDefinitions>
      <RowDefinition />
      <RowDefinition />
    </Grid.RowDefinitions>
    <ListView IsVisible="{Binding AnyExpense}"></ListView>
    <Label Grid.Row="1" IsVisible="{Binding AnyExpense,Converter={StaticResource NegatedBoolConverter}}"
           Text="There is no expenses registered."
            HorizontalTextAlignment="Center" FontSize="Huge" FontAttributes="Bold"/>
</Grid>
</abstract:ViewBase>